# Examples

The fixed executable examples are the JSON fixtures in
`fixtures/cases/`. They are deliberately self-contained so the conformance
planner can consume them without another repository or a shared ledger.
